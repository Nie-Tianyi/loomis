[package]
name = "observability"
version = "0.1.0"
edition = "2024"

[dependencies]
tracing = { workspace = true }
provider = { path = "../../core/provider" }
engine = { path = "../../core/engine" }
memory = { path = "../../core/memory" }
