[package]
name = "agent-macros"
version = "0.1.0"
edition = "2024"
description = "NVIDIA OO Agents-style proc macros for loomis — #[derive(Agent)] and #[agent_impl]"

[lib]
proc-macro = true

[dependencies]
syn = { version = "3", features = ["full", "extra-traits"] }
quote = "1"
proc-macro2 = "1"
