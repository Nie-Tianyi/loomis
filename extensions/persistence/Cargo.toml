[package]
name = "persistence"
version = "0.1.0"
edition = "2024"
description = "Conversation persistence — save/load threads, thread naming, and auto-save hook"

[dependencies]
memory = { path = "../../core/memory" }
provider = { path = "../../core/provider" }
engine = { path = "../../core/engine" }
util = { path = "../../core/util" }
serde = { workspace = true }
serde_json = { workspace = true }
tracing = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
