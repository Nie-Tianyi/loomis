[package]
name = "util"
version.workspace = true
edition = "2024"
description = "Shared workspace utilities — small helpers used across core and extensions"

[dependencies]
time = { workspace = true }
